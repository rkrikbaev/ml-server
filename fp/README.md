# Intro

There are Faceplate-side files neccessary for the ML server integration that are not part of the Faceplate codebase and stored in the project instead. They are version-controlled in this directory under the ML server project for now.
